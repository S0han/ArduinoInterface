import React from 'react';

const MessageEntryButton = ({}) => (
    <div className='messagentrybutton'>
        <button>
            SHOW MESSAGE!
        </button>
    </div>
);

export default MessageEntryButton;